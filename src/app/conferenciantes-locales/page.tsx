'use client';

import TalkManager from '@/components/TalkManager';

export default function ConferenciantesLocales() {
    return (
        <div className="min-h-screen bg-gray-50">
            <TalkManager />
        </div>
    );
}
